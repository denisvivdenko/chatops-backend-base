services:
  api:
    build: .
    command: uvicorn chatops.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  worker:
    build: .
    command: python -m chatops.workers.worker
